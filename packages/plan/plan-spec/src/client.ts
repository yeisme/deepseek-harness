/**
 * Client-namespace projection of the plan-spec domain: a pure re-export of
 * the package's types outlet. Client code imports ONLY the client namespace.
 *
 * @module @deepseek-ai/dsh-plan-spec/client
 */

export type * from './types.ts'
